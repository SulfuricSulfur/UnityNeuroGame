using UnityEngine;
using System.Collections;

public class GameManager : MonoBehaviour {

    // Use this for initialization
    ObjectSpawn objSpawn;
    TrialSelect numTrial;//used for accessing the number of trials the user has selcted
    //int trialsLeft = 0;//the number of trials that are remaining
	void Start () {
	//objSpawn = new ObjectSpawn(2);
        //numTrial = new TrialSelect();
        //trialsLeft = numTrial.Trials;
    }
	
	// Update is called once per frame
	void Update () {
        //as long as there are trials left, the game will continue
            //objSpawn.ShowTarget();
           // objSpawn.DisplayObjects();

        
	
	}
}
